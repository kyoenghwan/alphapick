{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test-scraper": "ts-node test-scraper.ts",
    "test": "ts-node test-function.ts",
    "test-debug": "ts-node test-debug.ts",
    "test-firestore": "ts-node test-firestore.ts",
    "test-init": "ts-node test-init.ts",
    "gcp-build": "npx puppeteer browsers install chrome",
    "start:backend": "ts-node src/standalone_backend.ts"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "date-fns": "^4.1.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "puppeteer": "^21.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "private": true
}